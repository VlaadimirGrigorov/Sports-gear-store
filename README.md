# Sports-gear-store

## Configuration

To run this application, you will need to configure your Stripe API key.

### Stripe API Key

Set the `Stripe__SecretKey` environment variable to your Stripe secret key. For example:

```bash
export Stripe__SecretKey="sk_test_..."
```
